# Overstate v3.9 — OIDC completion

Status: **Final** (accepted by owner; staged implementation still needs
separate approval).

Scope note: this record covers the v3.9 decision set only. Accepting it does
not approve implementation or any later stage; each stage returns separately.

## Settled

- Scope: complete the existing OIDC flow (separate identities, group
  mapping, callback hardening, tests, operator docs). Local password
  login untouched; logout stays local-only.
- D4 — Group config: claim-name env plus admin/operator group lists;
  no JSON blob.
- D1 — Identity: match on issuer + subject pair; an SSO login never
  merges into a same-named local account.
- D2 — Roles: IdP group membership maps to viewer/operator/admin at
  login; no match means viewer.
- D3 — Logout: local session only; no provider sign-out.

- D5 — Pre-change SSO rows: orphan for admin cleanup on the Users
  page; no backfill from unstored claims.

## Open

- None. Decision tree exhausted; record accepted.

## Scope (Accepted)

- Deliverable: this record plus the issuer+subject identity key (new
  columns + migration), per-login group mapping, hardened callback,
  OIDC tests, and operator docs (`docs/sso.md`, env examples, compose
  block). Out of scope: LDAP, MFA changes, extra scopes, provider
  sign-out — and implementation itself, which needs a separate
  explicit request.
- Done means: D1–D5 recorded above, no open questions, owner explicitly
  accepts this text, record marked Final. Accepting the record never
  approves implementation; each later stage returns separately.
- Execution words ("go", "do it all") authorize only this boundary;
  anything wider needs new explicit approval or a follow-up issue.
