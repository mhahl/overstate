# Overstate v3.6 — Stabilize v3/v3.5

Status: **Final** (accepted by owner; staged implementation still needs
separate approval).

Scope note: this record covers the v3.6 decision set only. Accepting it does
not approve implementation or any later stage; each stage returns separately.

## Settled

- D1 — Pillar diff with no data: explicit "no data — capture a snapshot
  or compare live" state, never "Identical pillar" for missing data.
- D2 — Conformity live-table parity: same HTMX fragment + `hx-push-url`
  wiring as minions/jobs.
- D3 — Bulk selection across HTMX swaps: preserve checked ids across
  swaps (state outside the swapped node, re-applied after swap).
- D4 — Live-pause key: keep one shared `overstate-live` key, documented
  as the v3.5 preference.
- D5 — Run-form precedence: explicit note when bulk is ignored
  (saved/preset wins).
- D6 — Sort robustness: sort jobs by real `started_at`, align conformity
  reader vocabulary with statuses the writer emits.

## Open

- None. Decision tree exhausted; record accepted.

## Scope (Accepted)

- Deliverable: this record plus stabilizing edits in
  `overstate_ui/pillar.py`, `minions.py`, `jobs.py`, `states.py`,
  the live-table and palette templates, and regression tests in
  `tests/test_v3.py` / `tests/test_v35.py`. Out of scope: new pages or
  workflows, schedule-add, nodegroups, SLS editor, multi-user/eauth,
  master-config editing — and implementation itself, which needs a
  separate explicit request.
- Done means: D1–D6 recorded above, no open questions, owner explicitly
  accepts this text, record marked Final. Accepting the record never
  approves implementation; each later stage returns separately.
- Execution words ("go", "do it all") authorize only this boundary;
  anything wider needs new explicit approval or a follow-up issue.
