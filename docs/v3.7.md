# Overstate v3.7 — Add-schedule UI

Status: **Final** (accepted by owner; staged implementation still needs
separate approval).

Scope note: this record covers the v3.7 decision set only. Accepting it does
not approve implementation or any later stage; each stage returns separately.

## Settled

- Scope: add-schedule UI on the Schedules page (name, function, interval),
  on top of list + enable/disable/delete. No live-table rework.
- D1 — Interval model: value + unit (seconds/minutes/hours/days),
  no cron field.
- D2 — Duplicate names: reject with a message; never silently overwrite.
- D3 — Function input: required free text, no app-side allowlist; the
  Salt service-account list is the guard.

## Open

- None. Decision tree exhausted; record accepted.

## Scope (Accepted)

- Deliverable: this record plus an add-schedule form, a new operator-gated
  `POST /schedules/<mid>/add` route with validation + audit, `kwarg`
  support in `SaltClient.local`, and regression tests in
  `tests/test_phase6.py` style. Out of scope: live-table treatment of the
  schedule list, master schedules, edit-in-place, cron builder — and
  implementation itself, which needs a separate explicit request.
- Done means: D1–D3 recorded above, no open questions, owner explicitly
  accepts this text, record marked Final. Accepting the record never
  approves implementation; each later stage returns separately.
- Execution words ("go", "do it all") authorize only this boundary;
  anything wider needs new explicit approval or a follow-up issue.
